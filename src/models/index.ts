import { IconType } from "react-icons/lib";

export type status = {
    income: number,
    expense: number,
    total: number
}

export type resumes = {
    title: string,
    Icon: IconType,
    value: any
}